FROM python:3.11-slim

# 👇 adicione estas duas linhas logo depois do FROM
ARG CACHEBUST=0
ENV CACHEBUST=${CACHEBUST}

# ... resto do seu Dockerfile (RUN apt-get..., COPY requirements.txt, pip install, COPY . , etc.)
